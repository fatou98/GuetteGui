{% extends 'layoutberger.html.twig' %}

{% block content %}
 <!-- recent orders table -->
            <div class="panel">
                <div class="panel-body pn">
                    <div class="table-responsive">
                    <h2 style="text-align:center;">Liste de  mes bergeries</h2><hr>
                        <table class="table admin-form theme-warning tc-checkbox-1 fs13">
                            <thead>
                                <tr class="bg-light">
                                    <th class="text-center">Select</th>
                                    <th class="">Nom</th>
                                    <th class="">Adresse</th>
                                    
                                    <th class="text-right">Status</th>

                                </tr>
                            </thead>
                            <tbody>
                            {% for berg in bergeries %}
                                
                                <tr>
                                    <td class="text-center">
                                        <label class="option block mn">
                                            <input type="checkbox" name="mobileos" value="FR">
                                            <span class="checkbox mn"></span>
                                        </label>
                                    </td>
                                    <td class="">{{berg.libelle}}</td>
                                    <td class="">{{berg.adresse}}</td>
                                    <td class="text-right">
                                        <div class="btn-group text-right">
                                            <a href="javascript::void(0)"><i class="fa fa-trash-o" style="color:red;font-size:22px;"></i></a>
                                            <a href="javascript::void(0)"><i class="fa fa-edit" style="color:green;font-size:22px;"></i></a>
                                        </div>
                                    </td>
                                </tr>
                                                           {% endfor %}

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

{% endblock %}